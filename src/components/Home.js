var React = require( 'react' );
var LBContainer = require('../containers/LBContainer');

function Home(props){
  return(
    <div className="home">
      <LBContainer />

    </div>
  )
}

module.exports = Home;
